package org.fintech.paytech.mapper.account;

/*import org.fintech.budget.domains.account.models.Account;
import org.fintech.budget.domain.core.account.dto.AccountDTO;
import org.mapstruct.Mapper;
import org.mapstruct.Mapping;
import org.mapstruct.factory.Mappers;

@Mapper
public interface AccountMapper {

    AccountMapper INSTANCE = Mappers.getMapper(AccountMapper.class);

    @Mapping(target = "id", ignore = true) // Ingore ID for DTO conversion
    AccountDTO entityToDto(Account customer);
}*/
